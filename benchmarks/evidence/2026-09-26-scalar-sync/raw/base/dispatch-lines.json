{
  "rows": [
    {
      "lines": {
        "1442: guard_ok = bool(torch.stack(checks + norm_checks).all().item())": 10,
        "168: return bool(torch.isfinite(stacked).all().item())": 5
      },
      "step": 24,
      "total": 15
    },
    {
      "lines": {
        "1442: guard_ok = bool(torch.stack(checks + norm_checks).all().item())": 10,
        "168: return bool(torch.isfinite(stacked).all().item())": 5
      },
      "step": 25,
      "total": 15
    },
    {
      "lines": {
        "1442: guard_ok = bool(torch.stack(checks + norm_checks).all().item())": 10,
        "168: return bool(torch.isfinite(stacked).all().item())": 5
      },
      "step": 26,
      "total": 15
    }
  ],
  "source_sha256": "3836c2121d5bd94b9a70ac2e5299d4f7e9e76578425ed2a6d17c2012bed8269b",
  "torch": "2.12.1"
}
